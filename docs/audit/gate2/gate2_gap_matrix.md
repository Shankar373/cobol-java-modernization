# Gate 2 Forensic Gap, Bug, and Limitation Matrix (Post-Fix Status)

This matrix documents every finding identified during the zero-assumption forensic audit of the Gate 2 validation architecture and its verified resolution status.

| ID | Component | Finding | Evidence | Type | Severity | Fix Implemented | Status |
|---|---|---|---|---|---|---|---|
| **G2-BUG-01** | `modernize/native_generator.py` | Fixed-length padded string comparison in `EVALUATE` / `IF` conditions uses `Objects.equals(var, literal)` instead of space-padded comparison. | `INVENTORY01.cob`: `ITEM-STATUS PIC X(10) VALUE "ACTIVE"`. In Java: `item_status = "ACTIVE    "`. `Objects.equals` evaluated `false`, causing fallback to `CHECK`. | **BUG** | **P0** | Added `CobolFormatHelper.cobolEquals` supporting standard COBOL space-padded alphanumeric relational semantics across `_build_single_when_condition`, `_translate_condition`, and Level-88 methods. | **RESOLVED & VERIFIED** |
| **G2-BUG-02** | `cobol_migrate.py` (`_run_db_state_comparison`) | Hardcoded SQL column parsing assumes all tables have `CUST_ID` and `CUST_NAME`. | Line 4839 hardcoded `CUST_ID` and `CUST_NAME`. | **BUG** | **P1** | Dynamically parse column names from `INSERT INTO` or `CREATE TABLE` statements across SQL schemas. | **RESOLVED & VERIFIED** |
| **G2-BUG-03** | `cobol_migrate.py` (`_run_db_state_comparison`) | Table discovery only looks in `repo/data/*.sql`, missing standard `repo/sql/*.sql` and `repo/database/*.sql`. | Failed to discover SQL files residing under `repo/sql/`. | **BUG** | **P2** | Search `repo/data/`, `repo/sql/`, and `repo/database/` for `.SQL` schema files. | **RESOLVED & VERIFIED** |
| **G2-BUG-04** | `cobol_migrate.py` (`stage_report`) | Traceability manifest write path leaked into parent workspace directory (`os.path.dirname(self.out)`). | Lines 5851-5853 wrote to workspace root. | **BUG** | **P2** | Restricted `traceability_manifest.json` writes strictly within `self.out`. | **RESOLVED & VERIFIED** |
| **G2-GAP-01** | `cobol_migrate.py` (`stage_validate`) | Input file staging only copied `data/in/`, ignoring custom/nested input folders (`data/source/`, `data/input/`, `inputs/`, `datasets/`). | Modernized batch application failed to find input files on non-`data/in/` repos. | **GAP** | **P1** | Recursively mirror all non-output data directories (`data/in`, `data/source`, `data/input`, `inputs`, `datasets`) to `modernized/data/`. | **RESOLVED & VERIFIED** |
| **G2-GAP-02** | `cobol_migrate.py` (`resolve_input_file`) | Flat-file batch reader layout generator (`extract_raw_layout`) only matched hardcoded `RAW-*` prefix. | Generic COBOL 01/05 records got 0 fields mapped. | **GAP** | **P1** | Replaced hardcoded parser with generic 01/05/10 elementary item layout extractor with automatic PICTURE length calculation. | **RESOLVED & VERIFIED** |
| **G2-GAP-03** | `cobol_migrate.py` (`_normalize_text`) | Text comparison normalizer stripped trailing spaces, masking fixed-width record truncation. | Stripped trailing spaces across all lines. | **GAP** | **P2** | Preserves intra-record trailing spaces while normalizing line endings (CRLF -> LF) and trailing blank lines. | **RESOLVED & VERIFIED** |
| **G2-LIM-01** | `cobol_migrate.py` (`stage_validate`) | Batch execution completion in generic mode relied solely on log scraping sentinel. | 120s timeout ceiling on unbuffered logs. | **LIMITATION** | **P2** | Checked process exit code immediately (`proc.poll() == 0`), log sentinel, and input/output availability. | **HARDENED** |
| **G2-LIM-02** | `cobol_migrate.py` / CI | Real DB2 validation requires live DB2 z/OS / LUW instance (`REAL_DB2_MODE=1`). | Returns `H2_EMULATED` / `NOT_VERIFIED` without live DB2 credentials. | **LIMITATION** | **P2** | Explicitly documented and enforced fail-closed verdict ladder when external mainframe is unavailable. | **DOCUMENTED** |
| **G2-MOCK-01** | `MockSqlService.java` | Fallback mock SQL service used when live DB is unavailable. | Generated in target source tree. | **MOCK** | **P1** | Fail-closed ladder strictly denies production certification when `MockSqlService` mock data is active. | **VERIFIED FAIL-CLOSED** |
| **G2-CI-01** | `.github/workflows/ci.yml` | CI fast lane ignored key validation suites. | Ignored suites in fast-lane. | **CI** | **P1** | Gate 2 string semantics & non-bypass verification added to differential testing workflows. | **ADDRESSED** |
| **G2-DOC-01** | Documentation | Documentation claims complete automatic Gate 2 certification across all unseen repositories. | Generic input discovery details were missing. | **DOCUMENTATION** | **P2** | Documentation updated with exact input staging rules and supported layout structures. | **RESOLVED** |
